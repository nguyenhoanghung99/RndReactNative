import { useMemo } from "react"

export const useParseLastMessage = (content: string) => {
  const result = useMemo(() => {
    // const parseContent = JSON.parse(content)
  },[content])
  console.log('content', content)

  return {}
}